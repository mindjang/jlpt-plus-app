# Anki 학습법 비교 분석

## 📊 현재 구현 vs Anki 학습법

### ✅ 유사한 점

1. **SM-2 알고리즘 기본 구조**
   - `ease` 계수 사용 (기본 2.5, 최소 1.3)
   - `interval` 계산 방식: `interval = interval * ease`
   - 첫 복습: 1일, 두 번째: 3일, 이후: 이전 간격 × ease

2. **Grade 시스템**
   - `again`, `hard`, `good`, `easy` 4단계 평가
   - `ease` 조정: hard(-0.15), easy(+0.15), good(변화 없음)

3. **Leech 처리**
   - `lapses >= 5` 시 `suspended = true`

4. **데이터 구조**
   - `reps`, `lapses`, `interval`, `ease`, `due`, `lastReviewed` 필드 사용

### ⚠️ 차이점 및 개선 필요 사항

#### 1. **"다시 학습" (Again) 버튼 동작**

**Anki 방식:**
- 학습 단계(Learning Steps) 개념 사용
- 기본 설정: `1분 → 10분 → 1일` (또는 사용자 커스터마이징 가능)
- "Again"을 누르면 학습 단계를 처음부터 다시 시작
- 각 단계를 통과하면 다음 단계로 진행

**현재 구현:**
- `30분` 고정 간격
- 학습 단계 개념 없음
- 단순히 30분 후에 다시 나타남

**개선 제안:**
```typescript
// 학습 단계 구현 예시
const LEARNING_STEPS = [1, 10, 1440] // 1분, 10분, 1일 (분 단위)

if (params.grade === 'again') {
  card.lapses += 1
  // 학습 단계를 처음부터 다시 시작
  card.learningStep = 0
  card.interval = LEARNING_STEPS[0] // 1분
  card.due = nowMinutes + LEARNING_STEPS[0]
  card.ease = Math.max(MIN_EASE, card.ease - 0.2)
  return card
}

// Good 버튼을 누르면 다음 학습 단계로
if (card.learningStep < LEARNING_STEPS.length - 1) {
  card.learningStep += 1
  card.interval = LEARNING_STEPS[card.learningStep]
  card.due = nowMinutes + card.interval
}
```

#### 2. **새 카드 첫 학습 후 복습 간격**

**Anki 방식:**
- 새 카드는 학습 단계를 거쳐야 함
- 첫 학습 후 "Good" → 1분 후, "Good" → 10분 후, "Good" → 1일 후
- 모든 학습 단계를 통과해야 복습(Review) 모드로 전환

**현재 구현:**
- 첫 학습 후 바로 1일 후 복습
- 학습 단계 없이 바로 복습 모드로 전환

**개선 제안:**
- 학습 단계(Learning Steps) 도입 필요
- `UserCardState`에 `learningStep` 필드 추가 고려

#### 3. **간격 단위 혼용**

**Anki 방식:**
- 학습 단계: 분 단위 (1분, 10분 등)
- 복습 간격: 일 단위 (1일, 3일, 7일 등)
- 명확한 구분

**현재 구현:**
- 모든 간격을 분 단위로 저장
- `interval < 1440분`이면 첫 복습으로 처리하는 복잡한 로직

**개선 제안:**
- 학습 단계와 복습 간격을 명확히 구분
- `isLearning: boolean` 플래그 추가 또는 `learningStep` 필드로 구분

#### 4. **"Hard" 버튼 동작**

**Anki 방식:**
- Hard 버튼은 ease를 낮추고 간격을 조금 줄임
- 하지만 학습 단계에서는 다음 단계로 진행 (Good과 동일)

**현재 구현:**
- Hard 버튼도 Good과 동일하게 간격 계산
- ease만 조정

**개선 제안:**
- 학습 단계에서는 Hard도 다음 단계로 진행
- 복습 모드에서만 Hard의 특별한 처리 적용

#### 5. **마이그레이션 로직 복잡성**

**현재 구현:**
- 일 단위와 분 단위를 구분하기 위한 복잡한 조건문
- `due < 10000`, `interval < 365` 같은 임의의 임계값 사용

**개선 제안:**
- 데이터 버전 필드 추가 (`version: number`)
- 또는 마이그레이션 완료 후 해당 로직 제거

## 🎯 우선순위별 개선 사항

### 높은 우선순위

1. **학습 단계(Learning Steps) 구현**
   - "다시 학습" 버튼이 학습 단계를 처음부터 다시 시작하도록
   - 새 카드가 학습 단계를 거치도록

2. **데이터 구조 확장**
   - `learningStep` 필드 추가 또는 `isLearning` 플래그
   - 학습 단계와 복습 모드 명확히 구분

### 중간 우선순위

3. **"Hard" 버튼 동작 개선**
   - 학습 단계에서는 Good과 동일하게 처리
   - 복습 모드에서만 특별한 처리

4. **마이그레이션 로직 정리**
   - 데이터 버전 관리 도입
   - 불필요한 조건문 제거

### 낮은 우선순위

5. **사용자 커스터마이징**
   - 학습 단계 간격 사용자 설정 가능
   - 기본값: 1분, 10분, 1일

## 📝 결론

현재 구현은 **SM-2 알고리즘의 기본 구조는 잘 따르고 있으나**, Anki의 핵심 기능인 **학습 단계(Learning Steps)** 개념이 빠져있습니다. 

가장 중요한 개선 사항은:
1. 학습 단계 도입으로 "다시 학습" 버튼이 더 효과적으로 동작하도록
2. 새 카드가 학습 단계를 거쳐 자연스럽게 복습 모드로 전환되도록

이러한 개선을 통해 Anki와 더 유사한 학습 경험을 제공할 수 있습니다.

